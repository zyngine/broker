# ── Discord Bot Credentials ───────────────────────────────────────────────────
# Get these from https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here
CLIENT_ID=your_application_client_id_here

# ── GUILD_ID (development only) ───────────────────────────────────────────────
# Set this ONLY for local dev/testing. Guild commands deploy instantly to one server.
# Leave UNSET in production (Railway) — commands will deploy globally to all servers.
# GUILD_ID=your_discord_server_id_here

# ── Database ──────────────────────────────────────────────────────────────────
# On Railway: automatically injected when you add a PostgreSQL service.
# Local format: postgresql://username:password@localhost:5432/broker
DATABASE_URL=postgresql://postgres:password@localhost:5432/broker

# ── Environment ───────────────────────────────────────────────────────────────
# Set to "production" on Railway. Enables SSL for the database connection.
# NODE_ENV=production

# ── Web Dashboard ─────────────────────────────────────────────────────────────
# Password to access the web dashboard at your Railway URL.
# Leave unset to disable the web dashboard entirely.
# DASHBOARD_PASSWORD=your_secure_password_here

# PORT is auto-injected by Railway. No need to set it manually.
